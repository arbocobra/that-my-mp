import { Inter, Lusitana, Lexend } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'] })
export const lexend = Lexend({ subsets: ['latin'], weight: ['100', '300', '500', '700'] })